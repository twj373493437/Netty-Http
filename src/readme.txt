
静态文件支持

注解式路由，转发

数据绑定，
单个数据(不用注解)，对象(必须使用注解@Params)

静态文件的缓存，不用从硬盘读，以便更好的支持web项目的小文件，lru 读写
锁

cookie

session(接口化，以便替换其他实现方式) 默认基于本地内存实现

过滤器（拦截器）实现 (还没实现指定url 拦截)

todo list

基于事件的处理模式

//可选(注解)的线程池异步调用，防止work线程用完

//websocket 的支持(暂时不实现)s

//MVC(基于freemarker)(暂不实现)

//http2 实现和测试(暂不实现)

//数据校验 (暂不实现)